## 基于TLE和序列观测图像的在轨卫星的轨道-姿态联合估计

#### 动机

一旦卫星做了：

- 轨道机动（**变轨**、轨道保持、交会制导等）

- 姿态大变化导致有效阻力面积变化（比如**展开大帆板、改变姿态**来增加/减小阻力）

- 喷气脱轨、碰撞碎片等异常

但是轨道库还没更新 TLE 时：

旧 TLE 还是当它“没动过”，轨道会沿着旧的趋势继续推；

实际轨道已经偏离很多了，误差可能直接跳到几十、几百公里甚至完全不靠谱。

目的是 **用自己的观测去补足精度和时效性。**

**单一传感器的信息是有限的**

- 只用 TLE：只能大致知道“在哪儿”，不知道“怎么转、干了什么”；

- 只用图像/雷达：缺几何背景，很难从亮点/轮廓直接推轨道和姿态。
   → 融合 TLE（几何先验）+图像（高精度/丰富观测）刚好互补。

  

TLE 更新频率/TLE本身就是通过

TLE 是按 **观测数据 + 轨道拟合** 得到的：

1. 地面雷达/光学望远镜等测站不断观测卫星；

2. 用这些观测做 **轨道确定（OD）**，求出轨道根数；

3. 然后再把轨道根数包装成 TLE 这种简单格式（适配 SGP4 模型）；

4. 最后发布到公开星历库（比如 CelesTrak、Space-Track）。

   

#### 数据的时间统一问题？

 这里TLE （好获得）对应的ISAR/光学图像怎么获取
再说通过我们仿真ISAR/光学图像，那对应的TLE在哪里



#### RAE

这里的 **RAE 一般就是 Range–Azimuth–Elevation**，也就是：

- **R：Range 距离**（站–星直线距离）

- **A：Azimuth 方位角**(卫星“在地平线上的投影”相对于某基准方向（通常是**正北**）的角度)

- **E：Elevation 仰角**

- **TLE → r,v → RAE**

  

**用 TLE 做轨道先验 + 用地基观测（RAE、ISAR、光学）去反演更精确的轨道和姿态 / 状态，从而实现在轨卫星的持续监测与异常发现**

目前的想法 只想把TLE当作先验，